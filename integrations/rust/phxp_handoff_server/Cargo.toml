[package]
name = "phxp-handoff-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
nix = { version = "0.30", features = ["fs", "socket", "uio", "user"] }
rustls = "0.23"
rustls-pemfile = "2"
sha2 = "0.10"

[workspace]
