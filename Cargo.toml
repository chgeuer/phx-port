[package]
name = "phx-port"
version = "0.1.0"
edition = "2024"
description = "Central port registry for Phoenix projects"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
native-tls = "0.2"
toml_edit = "0.22"
