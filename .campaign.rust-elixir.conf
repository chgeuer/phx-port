# shellcheck shell=bash disable=SC2034

CAMPAIGN_NAME="phx-port Rust and Elixir audit - Astra"
CAMPAIGN_PROJECTS=(.)
export PHX_AUDIT_QUEUE="audit-rust-elixir-2026-09-astra"
CAMPAIGN_LABELS=("$PHX_AUDIT_QUEUE")
CAMPAIGN_PROMPT_FILE="prompt.rust-elixir.md"
CAMPAIGN_GUARDRAILS="Work only the selected PHX_AUDIT_QUEUE under epic phx-27e. Preserve CONTEXT.md and the accepted ADRs, exact certificate-verified routes, workload-owned TLS, loopback-only backends, authenticated bounded PHXP and irreversible descriptor ownership, rootless production operation, bounded resources, and zero-bootstrap development. Start with E5 when ready. Never run the known freezing in-VM sender harness without an external watchdog. Never touch the old canary, production services, real private keys, or the existing dirty Mac checkout. Commit only explicit owned paths, without Co-authored-by trailers. Stop on a provider refusal; do not retry it with another model."

COPILOT_MODEL="gpt-6-astra"
COPILOT_EFFORT="max"
COPILOT_FALLBACK_MODEL=""
COPILOT_FALLBACK_EFFORT="max"
export PHX_AUDIT_MODEL="$COPILOT_MODEL"
