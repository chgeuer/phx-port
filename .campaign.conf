# shellcheck shell=bash disable=SC2034

CAMPAIGN_NAME="phx-port public ingress implementation"
CAMPAIGN_PROJECTS=(.)
CAMPAIGN_LABELS=(implementation-2026-09)
CAMPAIGN_PROMPT_FILE="prompt.md"
CAMPAIGN_GUARDRAILS="Follow CONTEXT.md, the accepted public-hosting design, and ADRs. Never weaken TLS hostname verification, PHXP peer authentication or descriptor ownership, loopback-only routing, resource bounds, rootless production execution, local control authentication, or zero-bootstrap development behavior. Never claim platform or load support without executable evidence. Work on exactly one non-epic issue and do not attempt a HITL issue without its required operator environment."

COPILOT_MODEL="${COPILOT_MODEL:-gpt-5.6-sol}"
COPILOT_EFFORT="${COPILOT_EFFORT:-max}"
COPILOT_FALLBACK_MODEL="${COPILOT_FALLBACK_MODEL-claude-opus-5}"
COPILOT_FALLBACK_EFFORT="${COPILOT_FALLBACK_EFFORT:-max}"
